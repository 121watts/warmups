class AppleMatcher

  def self.match(letters)
    array   = letters.chars
    counter = array.count
    #fill an array with the possiible combinations of rotations
    #compare letters to each element of said array
    #if all false not a rotation
    #if a true it is a rotation

    array.counter.times do
    p  array.rotate!
    end
    true
  end



end
